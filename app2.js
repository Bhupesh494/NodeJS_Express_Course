const { readFile, writeFile } = require("fs");

const getText = (path) => {};

const os = require("os");
os.uptime();

const os = require("os");

// write hello world program
let as = "hello world";

console.log(as);
